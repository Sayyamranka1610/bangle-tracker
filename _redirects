# Cloudflare Pages redirect rules

# Serve the main app directly at the root URL — no redirect flash
/        /bangle_v19.html  200

# Short alias — /app also opens the main app
/app     /bangle_v19.html  200
