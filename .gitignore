# Claude Code worktrees (temp branches, not for sharing)
.claude/worktrees/
.claude/settings.local.json

# Order files (private business data)
*.xlsx
*.pdf

# Phase 2 build artifacts
v2/node_modules/
v2/dist/
v2/.env.local

# OS
.DS_Store
Thumbs.db

# Wrangler cache
.wrangler/
bangle-search-worker/.wrangler/

# Indexer temp files
indexer_checkpoint.json
embeddings.json
desc_checkpoint.json
descriptions.json
indexer_output.txt
desc_indexer_output.txt

# Context/handoff files (contain credentials — never commit)
BANGLE_SEARCH_CONTEXT.md

# Local environment files (contain API tokens — never commit)
*.env
.env
bangle-search-ai-worker/.env

# Catalogue temp / working image folders (not for sharing)
.tmp_bangle_check/
catalogue/generated/
catalogue/final_catalogue/
catalogue/backdrops/
catalogue/enhanced_bangles/

# Local preview helper (Claude verification server, not for deploy)
.preview-server.js
